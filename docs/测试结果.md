# 测试结果

最终回归时间：2026-09-15

## 构建检查

| 模块 | 命令 | 结果 | 说明 |
| --- | --- | --- | --- |
| backend | `mvn clean compile` | 通过 | Java 17，62个源码文件编译成功 |
| backend | `mvn test` | 通过 | Tests run: 1, Failures: 0, Errors: 0 |
| admin-web | `npm run build` | 通过 | Vite构建成功；存在chunk大于500KB的体积警告，不影响运行 |
| mobile-app | `npm run build:h5` | 通过 | uni-app H5构建成功；存在依赖循环访问提示和版本更新提示，不影响构建 |

## 接口冒烟

| 模块 | 验证项 | 结果 | 说明 |
| --- | --- | --- | --- |
| 后端基础 | `GET /api/hazards` | 通过 | 返回 `code=200`，当前20条隐患 |
| 工单基础 | `GET /api/work-orders` | 通过 | 返回 `code=200`，当前13条工单 |
| 知识库 | `GET /api/knowledge/documents` | 通过 | 返回 `code=200`，当前5个知识文档 |
| RAG | `POST /api/chat/ask` | 通过 | 返回 `code=200`，答案长度882，来源引用4条 |
| AI识别 | `/api/ai/analyze-image` | 待人工图片回归 | 需现场选择真实图片验证 |
| AI复核 | `/api/ai/review-images` | 待人工图片回归 | 需处理前后图片验证 |

## 页面回归

| 页面 | 验证项 | 结果 |
| --- | --- | --- |
| 移动端首页 | 入口可用、按钮适合移动端 | 构建通过，待浏览器人工确认 |
| 拍照上报 | 上传、识别、确认提交 | 接口具备，待真实图片人工回归 |
| 我的上报 | 空状态和列表状态 | 构建通过，待浏览器人工确认 |
| 我的工单 | 过滤处置人员、状态显示 | 接口冒烟通过，待浏览器人工确认 |
| PC隐患列表 | 列表、详情、审核 | 接口冒烟通过，待浏览器人工确认 |
| 工单管理 | 创建、开始、提交、审核 | 构建通过，待浏览器人工确认 |
| 航道隐患一张图 | 地图、Marker、筛选、详情跳转 | 构建通过，需高德Key现场验证 |
| Dashboard | 指标、图表、真实数据 | 构建通过，数据接口可用 |
| 知识库管理 | 上传、解析、删除、重新解析 | 知识库列表接口通过 |
| AI助手 | 多轮问答、来源引用、无答案兜底 | RAG问答冒烟通过 |

## 已确认事项

- 真实密钥不写入 `.env.example`。
- 后端配置通过环境变量读取AI Key、AI Base URL、模型、Embedding模型和MySQL密码。
- PC端和移动端均支持 `VITE_API_BASE_URL` 配置后端地址。
- 课程演示数据已单独放在 `docs/sql/demo_data.sql`，并注明不是生产数据。
